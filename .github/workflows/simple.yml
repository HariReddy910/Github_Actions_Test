name: my 1st workflow 

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "hi"
      - name: multiple script
        run: |
          node -v
          npm -v
